<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="@Url.Action("Index", "Home")">
            <img src="/assets/img/resourse/logo.png" alt="logo" width="90" class="me-2">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link fw-semibold text-uppercase px-3" href="@Url.Action("Index", "Home")">Trang chủ</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-semibold text-uppercase px-3" href="@Url.Action("about", "Home")">Giới thiệu</a>
                </li>
            </ul>

            <div class="d-flex align-items-center gap-3 ms-lg-3 mt-3 mt-lg-0">
                <a href="@Url.Action("cart", "Home")" class="btn btn-outline-secondary position-relative" id="cartBtn" title="Giỏ hàng">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger x-cart-count" style="font-size:0.75em;">0</span>
                </a>

                <a href="@Url.Action("order", "Home")" class="btn btn-outline-secondary position-relative" id="orderBtn" title="Đơn hàng đã đặt">
                    <i class="fas fa-truck"></i>
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger x-order-count" style="font-size:0.75em;">0</span>
                </a>

                <div class="dropdown user-dropdown">
                    <a href="#" class="btn btn-outline-secondary dropdown-toggle" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user"></i>
                        @* Hiển thị tên nếu đã đăng nhập *@
                        @if (Session["TenKhachHang"] != null)
                        {
                            <span class="ms-1 small fw-bold">@Session["TenKhachHang"]</span>
                        }
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                        @if (Session["MaKH"] == null)
                        {
                            // CHƯA ĐĂNG NHẬP
                            <li><a class="dropdown-item" href="@Url.Action("register", "Home")">Đăng ký</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("login", "Home")">Đăng nhập</a></li>
                        }
                        else
                        {
                            // ĐÃ ĐĂNG NHẬP
                            <li><a class="dropdown-item" href="#">Hồ sơ của tôi</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("order", "Home")">Đơn mua</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="@Url.Action("Logout", "Home")">Đăng xuất</a></li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
